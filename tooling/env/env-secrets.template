# heypogi secrets - fill in values, never commit this file
# chmod 600 is set by setup-env.sh

# Paseo daemon password (required for auth)
#PASEO_PASSWORD=

# GitHub PAT for Copilot reviews
#GH_PAT_COPILOT=

# OpenCode server password (required when binding non-loopback)
#OPENCODE_SERVER_PASSWORD=

# OpenChamber UI password (required when binding non-loopback)
#OPENCHAMBER_UI_PASSWORD=
